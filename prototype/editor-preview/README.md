# Markdown editor and math preview prototype

Three structural variants of the Notes editing surface, switchable with `?variant=A`, `?variant=B`, or `?variant=C` on one throwaway Vite route.

Run from the repository root:

```powershell
npm run prototype
```

This code exists only to decide the MVP editing experience. It is not production application architecture.
