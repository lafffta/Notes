{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "User-selected Vault access for the throwaway shell prototype",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:read-files",
    "fs:read-dirs",
    "fs:allow-watch",
    "fs:allow-unwatch"
  ]
}
